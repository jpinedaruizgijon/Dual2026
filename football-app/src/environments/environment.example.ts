// ============================================================
// FICHERO DE EJEMPLO — copia esto como environment.ts
// y rellena los valores con tus propias claves.
// Este fichero SÍ se sube al repo (no tiene datos reales).
// ============================================================
export const environment = {
  production: false,

  // URL del proxy local (no cambiar en desarrollo)
  footballApiUrl: '/api-football',

  // Consigue tu API Key gratis en: https://www.football-data.org/client/register
  footballApiKey: 'TU_API_KEY_AQUI',

  // URL del backend Laravel en local
  apiUrl: 'http://localhost:8000/api'
};
